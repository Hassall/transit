from distutils.core import setup

setup(name='Transit',
      version='1.0',
      description='HTTP Request metrics',
      author='Haleem Assal',
      packages=['transit'],
      install_requires = ['pytest']
     )